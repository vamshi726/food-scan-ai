apiVersion: v1
kind: Secret
metadata:
  name: nutriscan-secrets
  namespace: nutriscan
  labels:
    app: nutriscan
type: Opaque
stringData:
  # Replace with your actual Supabase publishable key (base64 encoded in production)
  VITE_SUPABASE_PUBLISHABLE_KEY: "YOUR_SUPABASE_PUBLISHABLE_KEY_HERE"
